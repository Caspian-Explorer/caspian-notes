# Pin all text files to LF in the repo and on disk.
# Neutralizes core.autocrlf=true on Windows machines.
* text=auto eol=lf

# Binary assets — never normalize.
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.svg   binary
*.woff  binary
*.woff2 binary
*.vsix  binary
